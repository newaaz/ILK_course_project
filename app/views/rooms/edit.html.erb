<section class="pb-0">
	<div class="container">
		<div class="row">
			<div class="col-12 text-center">
				<h1 class="fs-3 mb-2"><%= @room.title %></h1>
				<p class="fs-5 mb-0">Изменение номера</p>
			</div>
		</div>
	</div>
</section>

<section>
	<div class="container">
    <div class="row">
      <div class="col-lg-12 mx-auto" id='room_form'>
        <%= render partial: 'form', locals: { property: @property, room: @room } %>
      </div>
    </div>
	</div>
</section>

