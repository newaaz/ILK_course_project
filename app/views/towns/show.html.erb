<h1><%= @town.name %></h1>

<%= image_tag @town.avatar.variant(resize_to_limit: [900, 200]) %>

<%= link_to "Properties in #{@town.parent_name}", hotels_town_path %>

<% @properties.each do |property| %>
  <%= link_to property.title, property %>
  <br>
<% end %>


