<% unless @status == :error %>
  <%= turbo_stream.update('favorite_main_menu', partial: 'shared/favorite_main_menu') %>
  <%= turbo_stream.update("add_favorite_#{dom_id @listing}", partial: 'shared/add_favorite', locals: { listing: @listing }) %>
<% end %>
