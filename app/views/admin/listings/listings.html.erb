<div class="container mt-4">
  <h3><%= controller.action_name.capitalize %></h3>

  <div class="row gy-3 mt-4">
    <%= render partial: 'admin/listings/listing', collection: @listings, as: :listing %>

    <% if @pagy.count > 12 %>
      <div class="col-12 mt-5 d-flex justify-content-center">
        <%= pagy_bootstrap_nav(@pagy).html_safe %>
      </div>
    <% end %>
  </div>
</div>

